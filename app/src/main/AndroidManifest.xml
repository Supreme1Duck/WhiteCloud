<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:icon="@drawable/ic_cloud"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.SingInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AssistantActivity" />
        <activity android:name=".animation.motion_activity" />
        <activity android:name=".activities.other_fragment_activities.WorkersActivity" />
        <activity android:name=".activities.other_fragment_activities.ClientsActivity" />
        <activity android:name=".activities.other_fragment_activities.LogOutActivity" />
        <activity android:name=".activities.other_fragment_activities.ManageActivity" />
        <activity android:name=".activities.other_fragment_activities.UpdateWorker" />
        <activity android:name=".MapboxMap"/>
    </application>

</manifest>